import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, text, boolean, radios } from '@storybook/addon-knobs';

import { SCAvatar } from '../../components';

<Meta title="Avatar" component={SCAvatar} />

# Avatar

## Displays an avatar to show up image/default placeholder

<Preview withToolbar>
    <Story name="Basic">
        <div style={{display: "grid", gridRowGap: "10px"}}>
            <SCAvatar size="xs" text="A" className="rounded" />
            <SCAvatar size="sm" icon="icon-done-outline" />
            <SCAvatar size="md" text="Sssss" />
            <SCAvatar size="lg" image="https://cdn.pixabay.com/photo/2015/06/19/21/24/the-road-815297_960_720.jpg" />
        </div>
    </Story>
</Preview>

### Playground

Experiment with this story with Knobs addon in Canvas mode.

<Story name="Playground" decorators={[withKnobs]}>
    <SCAvatar
        size={radios('size', {xs: 'xs', sm: 'sm', 'md': 'md', lg: 'lg'}, 'sm')}
        icon={text('icon', '')}
        image={text('image', '')}
        text={text('text', '')}
    />
</Story>
